# DESIGN.md

## 目的
このファイルは「労働衛生ラボ」のUI/UXルールです。
Codexや開発者がページ・ツール・記事UI・ナビゲーションを変更するときは、最初に参照してください。

目標は次の3つです。
- 信頼できる専門サイトに見える
- 初心者でも読みやすい
- スマホでも迷わず使える

装飾より、情報の優先順位・可読性・操作性を優先します。

---

## 最優先ルール

### MUST
- UI変更前に `assets/css/main.css` と `assets/css/components.css` の既存パターンを確認する。
- 既存のCSS変数・コンポーネントを優先して使う。
- 360px前後のスマホ幅でも、読める・押せる・横にはみ出さない状態を保つ。
- ボタンや主要リンクのタップ領域は原則44px以上にする。
- 色だけで状態やエラーを伝えない。
- 恒久的なデザインルールを変える場合は、この `DESIGN.md` も同じ変更で更新する。

### SHOULD
- 1画面で「何が重要か」が分かる視覚的階層にする。
- カード・影・ピル型ラベル・アイコンは必要な場所だけに使う。
- 新しいCSSを書く前に、既存クラスの再利用・派生で対応できないか確認する。
- PC版を縮小するだけではなく、スマホで自然な1列構成を優先する。
- 法令・制度・数値を扱うページは、装飾より根拠・読みやすさを優先する。

### AVOID
- すべてをカード化する
- 強い影を多数使う
- glassmorphism、ネオン、強いグロー
- 大面積の派手なグラデーション
- 巨大なアイコンや意味のないAIイラスト
- SaaSダッシュボード風の密集UI
- PCだけで成立するhover依存操作
- スマホで無理に2列・3列を維持する

---

## デザインの方向性
キーワード:
- clean
- technical
- calm
- practical
- trustworthy
- human-readable

基本は「専門情報を安心して読める」見た目にします。
目立たせるための装飾より、見出し・余白・コントラストで差をつけます。

---

## 色

既存の `assets/css/main.css` のCSS変数を使います。

| 用途 | Token | Value |
| --- | --- | --- |
| ページ背景 | `--color-bg` | `#f4f7fa` |
| 白背景 | `--color-surface` | `#ffffff` |
| 補助背景 | `--color-surface-muted` | `#edf3f7` |
| 本文 | `--color-text` | `#152238` |
| 補足文 | `--color-text-secondary` | `#48586c` |
| 弱い補足 | `--color-text-muted` | `#6b7a8d` |
| メイン | `--color-primary` | `#103b5c` |
| リンク | `--color-link` | `#0b6093` |
| 青の補助背景 | `--color-blue-soft` | `#eaf3f8` |
| アクセント | `--color-accent` | `#e0a12d` |
| アクセント文字 | `--color-accent-strong` | `#8b5700` |
| 黄の補助背景 | `--color-accent-soft` | `#fff4da` |
| 境界線 | `--color-border` | `#d3e0e9` |

### 色のルール
- 基本は **白 + ネイビー/ブルー + 黄を少量**。
- 黄は「重要箇所の目印」に使い、広い面積の主背景にはしない。
- 新しい色を直接追加する前に既存トークンで表現できないか確認する。
- 恒久的な新色が必要ならCSS変数として追加し、この表にも追記する。

---

## 文字

Font stack:
`-apple-system, BlinkMacSystemFont, "Hiragino Sans", "Yu Gothic", Meiryo, system-ui, sans-serif`

基準:
- 本文: 16px / line-height 1.7–1.8
- 記事本文: 17px / line-height 約1.82
- 補足情報: 13–14px
- カード見出し: 18–20px
- セクション見出し: 23–30px
- ページタイトル: 30–46px、レスポンシブ

ルール:
- 本文を不必要に太字にしない。
- H2 → H3 の階層を崩さない。
- 英字の大文字やletter-spacingを装飾目的で多用しない。
- 記事本文幅は原則 `--article: 760px` を基準にする。

---

## 余白とレイアウト

余白スケール:
`4 / 8 / 12 / 16 / 24 / 32 / 40 / 52 / 64px`

基準:
- Main container: `--container: 1120px`
- Article: `--article: 760px`
- 補助的な狭いページ: 820–960px程度
- PC: 必要に応じて2〜3列
- スマホ: 原則1列

同じ階層の要素は、同じ余白ルールを使います。
セクション間の余白は、カード内部の余白より明確に広くします。

---

## 既存コンポーネントを優先する

新しいUIを作る前に、以下の既存クラスを確認します。

### カード
標準候補:
- `.card`
- `.guide-card`
- `.entry-card`
- `.update-card`
- `.product-card`
- `.qa-list-card`

使う場面:
- 記事やカテゴリなど、クリック可能な独立項目
- 比較可能な複数項目
- 独立したツール・機能

使わない場面:
- 単なる本文
- 見出しを囲うだけ
- すべてのセクション
- 空間を埋めるためだけの箱

### ボタン
主要CTAの既存候補:
- `.cta-secondary`
- `.btn-step-primary`
- `.join-cta-primary`

補助CTAの既存候補:
- `.cta-tertiary`
- `.btn-step`
- `.join-cta-secondary`

同じ画面に、同じ強さの主要CTAを大量に置かない。

### ラベル・バッジ
既存候補:
- `.badge`
- `.guide-tag`
- `.tag-chip`
- `.article-category`
- `.status`

カテゴリ・状態など、短い意味がある場合だけ使います。
すべての見出しにピル型ラベルを付けません。

### 注意・補足
- 情報: `--color-blue-soft` + 左ボーダー
- 注意: `--color-accent-soft` + 黄系ボーダー
- 既存例: `.notice-box`, `.article-summary-box__panel`

新しい「似た箱」を増やす前に、既存パターンを再利用します。

---

## ホームページ

ホームは「情報を探す入口」です。広告的なランディングページにはしません。

優先順位:
1. 何のサイトか
2. 主要テーマへの導線
3. 検索・ナビゲーション
4. 新着・代表記事
5. YouTube・note等の外部コンテンツ
6. 補足情報

同じ大きさのカードが延々と続く構成は避けます。
セクションごとに情報密度やレイアウトを変えて単調さを減らしますが、色・文字・角丸・影は統一します。

---

## 記事

- 結論または概要を早めに示す。
- 長文はH2/H3で分割する。
- 法令・制度・数値は可能な限り一次情報へのリンクを示す。
- 更新日・確認日・注意書きは本文より弱い階層にする。
- 関連記事やYouTubeは本文の読了を邪魔しない位置に置く。

---

## ツール・計算機

基本順序:
**入力 → 結果 → 根拠・注意点**

- 入力欄を1画面に詰め込みすぎない。
- ラベルは入力欄の外に明示する。
- エラーは色だけで表現しない。
- 結果は目立たせるが、条件・根拠・免責を隠さない。
- スマホで片手操作しやすいサイズを保つ。

---

## アクセシビリティ

- `:focus-visible` を維持する。
- キーボード操作を壊さない。
- 文字サイズを極端に小さくしない。
- 色だけで意味を伝えない。
- 画像には適切なaltを設定する。
- 主要な操作対象は原則44px以上にする。

---

## 動き

- hover移動は1〜2px程度まで。
- transitionは150〜220ms程度。
- 常時アニメーションや派手なスクロール演出は原則使わない。
- 動きが内容より目立たないようにする。

---

## 判断が衝突したとき

優先順位は次の通りです。

1. アクセシビリティ・操作性・情報の正確さ
2. この `DESIGN.md` の恒久ルール
3. 既存コンポーネント・既存CSS
4. 個別ページの見た目上の都合

**恒久的なデザイン判断は `DESIGN.md` を優先します。**
既存CSSがこのファイルと矛盾する場合は、既存CSSを無条件に再利用せず、影響範囲を確認したうえで段階的に修正します。

---

## 実装ルール

1. UI変更前に既存CSS・クラスを確認する。
2. 既存CSS変数を優先する。
3. 新しいコンポーネントは既存パターンの派生で表現できないか確認する。
4. 見た目だけのためにJavaScriptを追加しない。
5. Jekyll/Liquidの既存構造を必要なく壊さない。
6. 360px前後のスマホ幅でも確認する。
7. 永続ルールを変えたら `DESIGN.md` も更新する。
8. 可能な範囲で `bundle exec jekyll build` と既存CIを通す。

---

## PRチェック

- [ ] 主要情報の優先順位が明確
- [ ] 既存コンポーネントを先に検討した
- [ ] 不要なカード・影・ラベル・アイコンを増やしていない
- [ ] 新しい色を不用意に増やしていない
- [ ] 360px前後で読める・押せる・横にはみ出さない
- [ ] タップ領域が小さすぎない
- [ ] キーボード操作・focus表示を悪化させていない
- [ ] 永続ルールを変更した場合は `DESIGN.md` も更新した

---

## Source of truth

- **DESIGN.md**: 「どう設計するか」の恒久的な判断基準
- **assets/css/main.css / components.css**: 現在の実装値
- **_layouts / _includes**: 現在の構造・コンポーネント実装

現在のCSSと `DESIGN.md` が一時的に食い違う場合は、恒久ルールとしては `DESIGN.md` を優先します。
ただし既存ページを一度に全面変更せず、影響範囲を確認しながら段階的に整合させます。
